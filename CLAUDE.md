# PROJECT_NAME

> Fill in the blanks below, delete this line, and leave the rest alone.
> Claude reads this file automatically at the start of every session —
> on your PC, your laptop, and your phone.

## What this is

**Client:** Reynaldo Wakit Binay-an (BinAI)
**What it does:**  Resume and portfolio showcasing automation projects of BinAI
**Status:** active
**Stack:** React 19 + TanStack Router/Query, Vite 7, TypeScript, Tailwind CSS 4, Radix UI, Framer Motion, Supabase (client), Vercel (deploy target), ESLint + Prettier

## Where the other half lives

Text files (code, workflow JSON, notes, prompts) live here in this repo.

Large assets — video, images, recordings, big PDFs — are **not** in this repo.
They live in Google Drive. Use the Google Drive connector to access them —
it works from any device, including when my PC is off.

## Credentials

No credentials, API keys, or `.env` contents are in this repo, and none should
ever be added to it. If a task needs one, ask me for it — don't go looking, and
don't write one into a file.

## How I work across machines

I use three devices: a Windows PC, a Windows laptop, and my phone. GitHub is the
only thing all three can see, so it's the source of truth.

**Please commit and push to `origin` whenever I command you to push.** Use a short, plain description of what changed. If I'm working
from my phone later, anything unpushed is invisible to me.

If a push fails because the remote has moved ahead, pull with rebase first, then
push again.

## Conventions

- Never commit anything over 50 MB — it belongs in Google Drive
- Keep `.gitignore` updated when new junk or build folders appear
- Workflow JSON exports go in `/workflows`
- Notes and docs go in `/docs`

## Notes

_Anything project-specific worth remembering between sessions:_

-
